Please help me update the code for our "tahap1-scraping" project. I need to make two major adjustments:

1. Data Type Casting:
Currently, the columns 'fee_standard', 'fee_subsidized', 'rating_score', 'rating_count', and 'count_attended' are all being saved as TEXT, which prevents them from being sorted properly. Please add data sanitization and casting before saving them to the database:
- 'fee_standard' and 'fee_subsidized' should be parsed into Numeric/Integer (remove currency symbols/commas if any).
- 'rating_score' should be parsed into Float.
- 'rating_count' and 'count_attended' should be parsed into Integer.

2. Headless Mode:
Modify the browser configuration to run in HEADLESS mode so that it runs in the background without opening a browser window.

Please provide the updated script or point out the exact lines I need to modify. Thanks!
