Hello Claude, please modify `scraper/extraction.py` and the database layer to merge `training_duration` and `training_duration_days` into a single field named `training_duration`. 

Apply these requirements:

1. Schema & Dataclass Update:
   - Remove the `training_duration_days` field completely from the CourseRecord dataclass, database schema, and upsert logic.
   - Keep only `training_duration`, which will now store either the hours or the days value.

2. Refactor Extraction Logic:
   - Combine the duration logic inside `extract_meta_footer` so that if a token contains "hours", "hrs", "day", or "days" (along with at least one digit), it gets assigned directly to `training_duration`.
   - Ensure that the `extract_duration_days_fallback` function is renamed to a generic fallback or integrated so that if `training_duration` is still missing after checking the elements, it scans raw text nodes for any mention of hours/days.

Please rewrite the updated functions inside `scraper/extraction.py` and align the database storage files accordingly.Hello Claude, please modify `scraper/extraction.py` and the database layer to merge `training_duration` and `training_duration_days` into a single field named `training_duration`. 

Apply these requirements:

1. Schema & Dataclass Update:
   - Remove the `training_duration_days` field completely from the CourseRecord dataclass, database schema, and upsert logic.
   - Keep only `training_duration`, which will now store either the hours or the days value.

2. Refactor Extraction Logic:
   - Combine the duration logic inside `extract_meta_footer` so that if a token contains "hours", "hrs", "day", or "days" (along with at least one digit), it gets assigned directly to `training_duration`.
   - Ensure that the `extract_duration_days_fallback` function is renamed to a generic fallback or integrated so that if `training_duration` is still missing after checking the elements, it scans raw text nodes for any mention of hours/days.

Please rewrite the updated functions inside `scraper/extraction.py` and align the database storage files accordingly.
