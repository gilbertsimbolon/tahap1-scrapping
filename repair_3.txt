Hello Claude, I need to make one final fix to the Stage 1 scraper in `scraper/extraction.py` and its corresponding storage schema. Please apply the following requirements carefully:

1. Rename Column:
   - Change the field name `telah_menghadiri` to `count_attended` across the entire project (including the CourseRecord dataclass, SQLite schema insertion, and extraction mapping).

2. Fix `training_duration_days` Extraction:
   - The current tag-based extraction loop in `extract_meta_footer` is returning NULL because the live HTML markup often splits the numbers and the word "days" into separate text nodes or injects hidden whitespaces/newlines.
   - Please refactor `extract_meta_footer` and introduce an aggressive fallback function called `extract_duration_days_fallback(soup)` that scans all raw text nodes (`soup.find_all(string=True)`) directly.
   - Clean up any raw whitespace in the text nodes using `" ".join(text_node.split()).strip()` and use a flexible regex pattern like `r'\b(\d+(?:\s*[-\u2013\u2014]\s*\d+)?\s*days?)\b'` to extract values like "3-5 days" or "2 days".
   - Call this fallback at the end of `extract_meta_footer` if the standard element loop fails to capture the duration days.

Please rewrite the updated functions in `scraper/extraction.py` cleanly and ensure the database upsert layers are perfectly aligned with the new `count_attended` column name!
